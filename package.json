{
  "name": "haile-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Haile — monorepo. /web = DUO (React/Vite). /packages/shared = lógica compartilhada. /supabase = backend único. (Dino vanilla segue em app/ e *.html na raiz.)",
  "workspaces": [
    "web",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "preview": "npm run preview -w web",
    "deploy:duo": "npm run build -w web && rm -rf duo && mkdir -p duo && cp -R web/dist/. duo/ && echo '[deploy:duo] built and copied to /duo/' "
  }
}
