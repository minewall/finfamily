.DS_Store
# Claude Code: ignora apenas arquivos local-only.
# settings.json e launch.json são versionados (defaults compartilhados).
.claude/settings.local.json
.claude/worktrees/
*.xlsx
gestor-financeiro-familiar.html
supabase/.temp/
pre-production/
pre_production_no_commit/
tmp/

# DUO (Node/Vite monorepo)
node_modules/
dist/
*.tsbuildinfo
.env.local
.env.*.local
# NOTA: /duo/ (build output produzido por `npm run deploy:duo`) é versionado
# durante a fase beta privada pra Cloudflare Pages servir estaticamente.
# Quando configurarmos CI/CD (build no CF Pages), gitignorar /duo/.
# Ver docs/deploy-duo.md.
